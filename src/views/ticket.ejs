<!DOCTYPE html>
<html lang="hr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Listić</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">
  <div class="container py-4">
    <div class="d-flex justify-content-between align-items-center mb-3">
      <h1>Listić</h1>
      <a href="/" class="btn btn-secondary">Početna</a>
    </div>
    <div class="card p-3">
      <p><strong>ID listića:</strong> <code><%= ticket.id %></code></p>
      <p><strong>ID kola:</strong> <code><%= ticket.roundId %></code></p>
      <p><strong>Broj osobne/putovnice:</strong> <%= ticket.idNumber %></p>
      <p><strong>Odabrani brojevi:</strong> <%= numbers.join(', ') %></p>
      <hr>
      <h5>Izvučeni brojevi</h5>
      <% if (draw) { %>
        <p><%= draw.join(', ') %></p>
      <% } else { %>
        <p class="text-muted">Još nema izvučenih brojeva za ovo kolo.</p>
      <% } %>
    </div>
  </div>
</body>
</html>
