<!DOCTYPE html>
<html lang="hr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Loto 6/45</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">
  <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container">
      <a class="navbar-brand" href="/">Loto 6/45</a>
      <div class="d-flex gap-2">
        <% if (user) { %>
          <span class="navbar-text text-white">Prijavljen: <%= user.name || user.email %></span>
          <a class="btn btn-outline-light btn-sm" href="/logout">Odjava</a>
        <% } else { %>
          <a class="btn btn-outline-light btn-sm" href="/login">Prijava</a>
        <% } %>
      </div>
    </div>
  </nav>

  <div class="container py-4">
    <div class="row g-3">
      <div class="col-12 col-lg-4">
        <div class="card">
          <div class="card-body">
            <h5 class="card-title">Status kola</h5>
            <% if (!currentRound) { %>
              <p class="text-muted">Nema podataka o kolima.</p>
            <% } else { %>
              <p>Trenutno kolo: <code><%= currentRound.id %></code></p>
              <p>Status: <span class="badge <%= currentRound.status === 'OPEN' ? 'text-bg-success' : 'text-bg-secondary' %>"><%= currentRound.status %></span></p>
            <% } %>
          </div>
        </div>
      </div>

      <div class="col-12 col-lg-4">
        <div class="card">
          <div class="card-body">
            <h5 class="card-title">Uplaćeni listići</h5>
            <% if (ticketCount === null) { %>
              <p class="text-muted">—</p>
            <% } else { %>
              <p>Broj listića u trenutnom kolu: <strong><%= ticketCount %></strong></p>
            <% } %>
            <% if (user) { %>
              <% if (currentRound && currentRound.status === 'OPEN') { %>
                <a href="/submit" class="btn btn-primary">Uplati listić</a>
              <% } else { %>
                <p class="text-muted">Uplate trenutno nisu omogućene.</p>
              <% } %>
            <% } else { %>
              <p>Za uplatu listića potrebno je <a href="/login">prijaviti se</a>.</p>
            <% } %>
          </div>
        </div>
      </div>

      <div class="col-12 col-lg-4">
        <div class="card">
          <div class="card-body">
            <h5 class="card-title">Izvučeni brojevi</h5>
            <% if (drawNumbers && drawNumbers.length) { %>
              <p><%= drawNumbers.join(', ') %></p>
            <% } else { %>
              <p class="text-muted">—</p>
            <% } %>
          </div>
        </div>
      </div>
    </div>
  </div>
</body>
</html>
